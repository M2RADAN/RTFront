import { NAVIGATIONS } from "../../const/constants";
import HeaderLink from "../../ui/HeaderLink";

const Profile = () => {
  return (
    <div>
      <HeaderLink route={NAVIGATIONS.LOGIN}>хуй</HeaderLink>
    </div>
  );
};

export default Profile;
